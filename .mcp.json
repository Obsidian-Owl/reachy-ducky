{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "github-mcp-server",
        "--read-only",
        "--toolsets",
        "pull_requests,issues,actions,repos"
      ]
    }
  }
}
